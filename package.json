{
  "name": "@wattrobert/vite-react-typescript-monorepo",
  "author": "Robert Watt",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "^20.11.0",
    "npm": "^10.2.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "npm run build -ws",
    "clean": "npx -y rimraf node_modules **/node_modules/ package-lock.json && npm i",
    "lint": "npm run lint -ws"
  },
  "workspaces": [
    "admin",
    "storefront",
    "core"
  ]
}
